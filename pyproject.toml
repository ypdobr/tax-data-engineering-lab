[project]
name = "tax-data-engineering-lab"
version = "0.1.0"
description = "Synthetic tax and finance data engineering lab for controlled workflows and AI-assisted review."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]
testpaths = ["tests"]

